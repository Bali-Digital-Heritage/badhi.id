<style>
    .logo-small {
        width: 100px;
    }

    .hero.is-fullheight {
        position: relative;
        overflow: hidden;
    }

    .video-container {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
        overflow: hidden;
    }

    .video-container video {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        min-width: 100%;
        min-height: 100%;
        width: auto;
        height: auto;
        object-fit: cover;
    }

    .text-shadow{
    text-shadow: 0px 2px 6px black;
    }
</style>

<section class="hero is-fullheight" id="temple">
    <div class="hero-body info-parent">
        <!-- video -->
        <div class="video-container">
            <video autoplay muted loop playsinline>
                <source src="/assets/Video-Sanggah.webm" type="video/webm">
                Your browser does not support the video tag.
            </video>
        </div>
        <!-- end video -->
        
        <div class="container is-max-desktop">
            <div class="columns is-vcentered text-shadow ">
                <div class="column">
                    <h1 class="title is-size-1 has-text-white-bis">{{ i18n[page.lang].temple.title }}</h1>
                </div>
                <div class="column is-two-thirds">
                    <p class="is-size-5 has-text-white-bis">
                        {{ i18n[page.lang].temple.maintext | safe}}                
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>